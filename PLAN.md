# 记账小程序 · 开发计划

> 2026-07-26 | 单 HTML 方案 | 部署 GitHub Pages

---

## 一、整体架构

**单 HTML 文件方案**：一个 `index.html` 包含全部 HTML + CSS + JavaScript。
依赖外部库（通过 CDN 引入）：
- `Chart.js` — 饼图/折线图（月度报告）
- 无其他依赖

## 二、文件结构

```
记账小程序/
├── index.html          ← 主应用（全部代码）
└── README.md           ← 项目说明（可选）
```

部署：推送到 GitHub 仓库 `mitanguse/accounting-app` 的 `main` 分支，GitHub Pages 自动上线。

## 三、数据设计（localStorage）

### 3.1 records — 记账记录

```javascript
[
  {
    id: "20260726_001",
    amount: 9.9,
    note: "咖啡",         // 用途
    category: "餐饮",      // 分类
    date: "2026-07-26",   // 日期 YYYY-MM-DD
    timestamp: 1785048850000
  }
]
```

### 3.2 categoryRules — 分类映射规则（自动学习）

```javascript
{
  "咖啡": "餐饮",
  "地铁": "交通",
  "手柄": "购物",
  // ... 用户每确认一次就新增或更新一条
}
```

### 3.3 budget — 预算设置

```javascript
{
  total: 5000,           // 月总预算，0表示不限制
  categories: {
    "餐饮": 2000,
    "交通": 500,
    "购物": 1000
    // 未设置的分类视为不限制
  }
}
```

## 四、核心逻辑模块

### 4.1 输入解析引擎

```
用户输入 "9.9 咖啡"
→ 正则匹配：/(\d+\.?\d*)\s*(.+)/ 
→ 提取 amount=9.9, note="咖啡"
→ 查 categoryRules → "咖啡" → "餐饮"
→ 匹配不到 → 弹出分类选择器 → 用户选定后存入 rules
→ 写入 records
→ 刷新列表
```

### 4.2 分类引擎（关键词匹配优先级）

1. 精确匹配 categoryRules 字典
2. 关键词包含匹配（如"午"匹配"午饭"→"餐饮"）
3. 兜底 → 弹出选择器让用户选

预置规则表（20+条初始映射）：

| 关键词 | 分类 |
|:------|:----|
| 咖啡/奶茶/水/饮料 | ☕ 餐饮 |
| 饭/餐/吃/外卖/食堂/面 | ☕ 餐饮 |
| 地铁/公交/打车/滴滴/加油/停车 | 🚇 交通 |
| 买/衣服/鞋/数码/京东/淘宝 | 🛒 购物 |
| 游戏/电影/会员/Steam/氪金/剧本杀/电玩/教父 | 🎮 娱乐 |
| 水电/电费/房租/话费/网费 | 🏠 日用 |
| 药/医院/看病/体检 | 💊 医疗 |
| 书/课程/培训/考试 | 📚 学习 |

### 4.3 月度报告生成

- 按当前月份过滤 records
- 按分类聚合计算总额
- 计算日均支出 = 总支出 / 已过天数
- 环比上月：对比上月同期总支出 + 各分类变化
- Chart.js 饼图展示分类占比

### 4.4 搜索模块

- 输入关键词 → 过滤 note 包含关键词的记录
- 分类下拉筛选
- 日期范围选择
- 结果列表展示

### 4.5 预算检查

- 每次新增记录时触发
- 计算当月已支出总额 → 对比 total 预算 → 超标标记红色
- 计算已支出各分类 → 对比分类预算 → 超标标记红色
- 超标时弹轻提醒（toast 通知）

## 五、UI 页面规划

### 页面切换方式

底部 Tab 导航，5个页面：

| Tab | 页面 | 说明 |
|:---|:----|:----|
| 📝 | 记账 | 默认首页，输入框 + 今日记录列表 |
| 📊 | 月报 | 饼图 + 日均 + 上月对比 + 最近12月趋势 |
| 🔍 | 搜索 | 搜索框 + 筛选 + 结果列表 |
| 💰 | 预算 | 总预算设置 + 分类预算进度条 |
| ⚙️ | 设置 | 分类规则列表（可编辑删除）+ 数据管理 |

### 移动端适配

- 所有按钮最小 44×44px（触控友好）
- 输入框字号 18px+，方便手机打字
- 底部 Tab 固定，不随页面滚动
- 颜色柔和护眼

## 六、开发步骤（分步执行）

```
Step 1: HTML 骨架 + CSS 样式 + 页面切换
Step 2: 数据层（localStorage CRUD）
Step 3: 输入解析引擎 + 自动分类引擎
Step 4: 记账页面完整功能
Step 5: 月度报告（Chart.js 饼图 + 月对比）
Step 6: 搜索功能
Step 7: 预算提醒功能
Step 8: 设置页（规则编辑）
Step 9: 综合测试 + 修复
```

## 七、验证标准

- [ ] 输入 "9.9 咖啡" → 自动记餐饮分类
- [ ] 输入 "5 地铁" → 自动记交通分类
- [ ] 输入未识别词 → 弹分类选择 → 选中后下次自动归类
- [ ] 月报页显示饼图和上月对比
- [ ] 搜索"咖啡" → 显示所有咖啡记录
- [ ] 预算超支 → 红色标记
- [ ] 全部数据在 localStorage，刷新不丢失
- [ ] 手机浏览器打开正常显示和操作
